spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://fopet.cies4vfjosiu.ap-northeast-2.rds.amazonaws.com:3306/forpet
    password: forpet1234
  jpa:
    properties:
      hibernate:
        format_sql: 'true'
        show_sql: 'true'
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
logging:
  level:
    org:
      hibernate:
        type:
          description:
            sql: trace
